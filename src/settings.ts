export default {
  // 是否有左侧菜单, true为dev和prd环境都显示， false为dev不显示，prd显示
  isLeftbar: true,
  // 左侧菜单是否可以收起
  isLeftMini: true,
  // 是否显示logo
  isLogo: false,
  // 是否有头部菜单
  // isTopbar: true,
  // light 默认 dark 可选, 目前的主题只影响到左侧菜单栏
  theme: 'light'
};
