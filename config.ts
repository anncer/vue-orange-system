export default {
  // 是否有左侧菜单
  isLeftbar: false,
  // 左侧菜单宽度px
  // leftWidth: "200",
  // 左侧菜单是否可以收起
  // isLeftMini: true,
  // 是否有头部菜单
  // isTopbar: true,
  // 头部菜单宽度
  // topHeight: "60",
  // theme: true
};
